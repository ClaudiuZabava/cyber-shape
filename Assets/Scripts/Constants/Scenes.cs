namespace Constants
{
    // Always make sure this enum has the scenes in the same order as they are in the build settings.
    public enum Scenes
    {
        MainMenuScene,
        Level1,
        Level2,
        OptionsMenu,
        GameOverMenu,
    }
}